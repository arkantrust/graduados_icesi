name: graduados-icesi
description: Mockup for the benefits module of the alumni project (ICESI).
publish_to: 'none'
version: 1.0.0
platforms:
  web:

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:

  flutter:
    sdk: flutter

  bloc: ^8.1.2
  equatable: ^2.0.5
  flutter_bloc: ^8.1.3

  go_router: ^12.0.1
  uuid: ^4.2.1
  google_fonts: ^6.1.0

  web: ^0.3.0
  material_color_utilities: ^0.5.0
  meta: ^1.10.0

dev_dependencies:

  flutter_test:
     sdk: flutter

  flutter_lints: ^3.0.1

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    # - assets/icons/
    # - assets/fonts/
